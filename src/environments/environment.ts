// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  apiURL:
    'https://animal-shelter-b8948-default-rtdb.europe-west1.firebasedatabase.app/',
  placeholderImg:
    'https://firebasestorage.googleapis.com/v0/b/animal-shelter-b8948.appspot.com/o/animals%2Fno_animal_pic.png?alt=media&token=d2269978-f927-4c51-ab6f-ae6950aaba97',
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
